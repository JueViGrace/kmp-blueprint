import kotlin.Boolean;

CREATE TABLE IF NOT EXISTS s2_session(
    id TEXT NOT NULL PRIMARY KEY,
    access_token TEXT NOT NULL,
    refresh_token TEXT NOT NULL,
    active INTEGER AS Boolean NOT NULL
);